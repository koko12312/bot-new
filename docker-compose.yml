version: '3.8'
services:
  bot:
    build: .
    container_name: whatsapp_rental_bot
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./users.db:/app/users.db
